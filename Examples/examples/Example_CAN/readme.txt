CAN_Example工程主要演示了 CAN收发数据流程。

硬件环境要求:
    1. BP15xx系列开发板 ；
    2. 外接串口小板，TX/RX/GND 接至 A9/A10/GND 引脚 (波特率 2000000)；
    3. 外接CAN分析仪，A3/A4，波特率500K

软件使用说明:
       上电启动之后，可通过串口输入不同指令，进入4种例程；
   1. 输入‘0’，进入循环接收测试;
   2. 输入‘1’，进入中断接收测试;
   3. 输入‘2’，进入发送测试;
   4. 输入‘3’，进入转发测试;
